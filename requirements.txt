requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
pdfplumber>=0.10.0
python-dateutil>=2.8.2
# Optional: Playwright enables auto-render of JS-driven IR pages
# (HDFC, ICICI, SBI). Engine still runs without it, falling through to NSE.
playwright>=1.45.0
